name: adriel_portfolio
description: Adriel Lewis L. Araos — Personal Portfolio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  file_picker: ^8.0.0
  flutter_staggered_grid_view: ^0.7.0
  cached_network_image: ^3.3.1
  go_router: ^13.0.0
  flutter_svg: ^2.0.9
  url_launcher: ^6.2.5
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  mime: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
